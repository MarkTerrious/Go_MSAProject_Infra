apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "auth-db.fullname" . }}-init-sql
data:
{{- range $path, $bytes := .Files.Glob "schema/*.sql" }}
  {{- base $path | nindent 2 }}: |
{{- $.Files.Get $path | nindent 4 }}
{{- end }}
