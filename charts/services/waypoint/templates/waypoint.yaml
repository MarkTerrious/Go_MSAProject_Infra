apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: board-service-waypoint
  annotations:
    istio.io/waypoint-for: service
spec:
  gatewayClassName: istio-waypoint
  listeners:
  - name: mesh
    protocol: HBONE
    port: 15008