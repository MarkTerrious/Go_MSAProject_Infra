apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "post-db.fullname" . }}-test-connection"
  labels:
    {{- include "post-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "post-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
