apiVersion: v1
kind: Service
metadata:
  name: {{ include "post-db.fullname" . }}
  labels:
    {{- include "post-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "post-db.selectorLabels" . | nindent 4 }}
